# Areas de mejora:

Críticas (impactan funcionalidad)
1. Cero tests — Los 3 tests.py están vacíos y no hay tests en el frontend. La lógica de overlap detection es crítica y debería estar testeada
2. (## HECHO ##) Validación de formulario incompleta — El frontend no valida que end_time > start_time antes de enviar, deja que el backend rechace
3. (## HECHO ##) Service layer duplicado — Hay reservationsService.js (mencionado en AGENTS.md) Y reservaService.ts + api.ts. Hay inconsistencia entre archivos JS y TS

# Importantes (deuda técnica)

4. (## HECHO ##)Frontend JS/TS mezclado — Pages y componentes en .jsx pero servicios en .ts. Debería ser todo TypeScript
5. Frontend preparado para auth pero sin backend — authService.ts apunta a /auth/login/ que no existe todavía
6. (## HECHO ##) CSS innecesario — index.css tiene estilos de template que son sobrescritos por App.css
7. Directorios vacíos — hooks/, routes/, styles/ sin contenido

# Menores (mejoras de UX/robustez)

8. ReservationItem tiene onDelete pero no hay DELETE endpoint — UI preparada para algo que no existe
9. Sin feedback visual al crear reserva — No hay mensaje de éxito, solo redirige
10. /api/users/ expuesto sin necesidad — Endpoint temporal que debería estar oculto o protegido